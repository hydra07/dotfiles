[project]
name = "translate-tool"
version = "0.1.0"
description = "Async batch translator CLI (deep-translator + httpx)"
requires-python = ">=3.14"
dependencies = [
    "deep-translator",
    "httpx[http2]",
]

[tool.uv]
package = false
